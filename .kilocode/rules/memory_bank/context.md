# Memory Bank - Context Log

## 2026-01-06 - Добавлена возможность редактирования данных за прошлые дни

### Обзор изменений
Добавлена функциональность для навигации по датам и редактирования данных за предыдущие дни. Это позволяет пользователям заполнять пропущенные дни.

### Изменения в API

#### `/api/day/rate` (POST)
- Добавлен опциональный параметр `date` в BodySchema
- Если `date` не указан, используется текущая дата (логическая дата пользователя)
- Позволяет сохранять оценки (efficiency, social) и заметки (journalText) для любой даты

#### `/api/habits/toggle` (POST)
- Добавлен опциональный параметр `date` в BodySchema
- Если `date` не указан, используется текущая дата (логическая дата пользователя)
- Позволяет отмечать/снимать отметки с привычек для любой даты

#### `/api/user/status` (GET)
- Добавлен опциональный query-параметр `date`
- Если `date` не указан, возвращаются данные для текущей даты
- Возвращает привычки, их статус выполнения, оценки и заметки для указанной даты

### Изменения в UI (`src/components/app-shell.tsx`)

#### Навигация по датам
- Добавлены кнопки навигации влево/вправо для переключения между днями
- Отображается текущая выбранная дата или "Сегодня"
- При выборе прошлой даты показывается кнопка "Вернуться к сегодняшнему дню"
- Дата подсвечивается цветом primary, если это прошлый день

#### Визуальная индикация режима редактирования
- В секции привычек появляется сообщение "Режим редактирования прошлых дней" при выборе прошлой даты
- Заголовки секций "Оценка дня" и "Итоги / заметка" добавляют "(редактирование)" при редактировании прошлых дней
- Placeholder в текстовом поле заметки меняется на "Редактируй заметку за этот день..."

#### Обновление данных
- Все функции сохранения (toggleHabit, setRating, saveJournal) теперь передают параметр `selectedDate` в API
- SWR кэш обновляется при смене выбранной даты
- Данные загружаются для выбранной даты через query-параметр `date`

### UX решения
1. **Интуитивная навигация**: Стрелки влево/вправо для перехода между днями
2. **Ясная индикация**: Цветовая подсветка и текстовые подсказки показывают режим редактирования
3. **Быстрый возврат**: Кнопка для быстрого возвращения к текущему дню
4. **Оптимистичные обновления**: UI обновляется мгновенно, затем синхронизируется с сервером

### Технические детали
- Используется состояние `selectedDate` (null = текущий день, string = YYYY-MM-DD)
- Функция `navigateDate(delta)` для перемещения на +/- дней
- Функция `goToToday()` для сброса на текущий день
- Переменная `isPastDate` определяет, находится ли пользователь в режиме редактирования прошлых дней

## 2026-01-06 - Добавлены напоминания о незаполненных днях

### Обзор изменений
Добавлена система напоминаний через Telegram бота для пользователей, которые забыли заполнить данные за предыдущие дни.

### Изменения в Cron endpoints

#### `/api/cron/remind` (POST) - Обновлен
- Теперь проверяет, были ли заполнены данные за вчерашний день
- Если данные отсутствуют (нет daily_log и нет habit_completions), отправляет персонализированное напоминание
- Сообщение: "Бро, ты забыл заполнить данные за вчера (YYYY-MM-DD)! 📊\n\nЗаполни пропущенные дни, чтобы не терять прогресс."
- Если данные есть, отправляет стандартное напоминание о заполнении текущего дня
- Возвращает статистику: sent, failed, missingData

#### `/api/cron/remind-missing` (POST) - Новый endpoint
- Проверяет последние 7 дней для каждого пользователя
- Находит все даты, за которые нет данных (нет daily_log и нет habit_completions)
- Отправляет напоминание только если есть незаполненные дни
- Сообщение включает список всех пропущенных дат в формате:
  ```
  Бро, ты забыл заполнить данные за эти даты:

  • YYYY-MM-DD
  • YYYY-MM-DD
  • YYYY-MM-DD

  Заполни их, чтобы не терять прогресс! 📊
  ```
- Включает кнопку WebApp "Заполнить пропущенные дни"
- Возвращает статистику: sent, failed, total

### Технические детали
- Использует `getLogicalDate()` для определения текущей даты пользователя с учетом часового пояса
- Проверяет наличие данных в таблицах `daily_logs` и `habit_completions`
- День считается незаполненным, если нет ни daily_log, ни habit_completions
- Оба endpoint требуют `x-cron-secret` header для авторизации
- Использует `Promise.allSettled` для параллельной обработки пользователей
